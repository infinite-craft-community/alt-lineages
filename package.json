{
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*"
    ],
    "catalog": {
      "wrangler": "^4.85.0"
    }
  },
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "format": "oxfmt",
    "build": "bun --cwd ./apps/userscript build",
    "deploy": "bun run --filter './apps/**' deploy"
  },
  "devDependencies": {
    "@gameroman/config": "^0.1.0",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0",
    "oxlint-tsgolint": "^0.22.0",
    "typescript": "^6.0.3"
  }
}
